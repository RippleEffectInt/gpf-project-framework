{
  "platform": {
    "apiRuntime": "node:20"
  },
  "routes": [
    {
      "route": "/api/projects",
      "allowedRoles": ["authenticated"]
    },
    {
      "route": "/api/projects/*",
      "allowedRoles": ["authenticated"]
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": [
      "/api",
      "/api/*",
      "/*.{css,js,json,png,jpg,jpeg,gif,svg,ico,woff,woff2}"
    ]
  }
}
