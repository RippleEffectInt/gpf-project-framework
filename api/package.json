{
  "name": "project-framework-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/functions/*.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "esbuild functions/projects.ts --bundle --platform=node --target=node20 --format=esm --outdir=dist/functions --packages=external",
    "test": "node --test"
  },
  "dependencies": {
    "@azure/functions": "^4.8.0",
    "@azure/msal-node": "^6.0.0",
    "node-forge": "^1.4.0"
  },
  "devDependencies": {
    "@types/node-forge": "^1.3.14",
    "esbuild": "^0.25.10"
  }
}
